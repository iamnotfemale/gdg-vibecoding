<div align="center">

# 바이브 코딩

**Claude Code로 웹 서비스 만들고 배포하기**

2026 GDGoC Korea Univ. 바이브코딩 특강(150분)의 공식 사이트

[**gdg-vibecoding.vercel.app**](https://gdg-vibecoding.vercel.app)

[![슬라이드](https://img.shields.io/badge/슬라이드-53장-801A2C)](https://gdg-vibecoding.vercel.app/slide/)
[![프롬프트](https://img.shields.io/badge/프롬프트-40개-801A2C)](https://gdg-vibecoding.vercel.app/prompt/)

</div>

<!-- 스크린샷: 메인 페이지 -->

---

## 제작 목적

코딩을 처음 하는 학부생 대상 특강입니다. 150분 안에 **아이디어부터 배포까지 한 사이클**을 손으로 끝내는 게 목표라, 강의 자료가 다음 세 가지를 동시에 해내야 했습니다.

**1. 발제와 실습이 끊기지 않을 것**
강의실 프로젝터로 슬라이드를 띄우는 동안, 수강생은 자기 노트북에서 같은 내용을 따라갑니다. 슬라이드에 프롬프트를 띄워놓고 받아 적게 하면 오타로 시간을 다 씁니다. 그래서 발제용 슬라이드와 **복사해서 쓰는 프롬프트 페이지**를 분리했습니다.

**2. 놓쳐도 혼자 따라올 수 있을 것**
한 단계라도 막히면 그 뒤가 전부 밀립니다. 프롬프트 페이지는 파트별로 접어두고, 각 단계마다 **실제 실행 화면 사진**을 붙여서 어디까지 왔는지 스스로 대조할 수 있게 했습니다.

**3. 결과물이 남을 것**
"만들었다"로 끝나지 않고 **인터넷에 열려 있는 내 주소**가 나와야 합니다. 쉬는시간에 서로 구경하는 공유 페이지를 두어, 배포가 강의의 하이라이트가 되도록 배치했습니다.

여기에 더해, 뭘 만들지 못 정한 사람을 위한 브레인스토밍 페이지와 불참자용 대체 과제 안내를 붙였습니다.

---

## 화면

| 페이지 | 하는 일 |
| --- | --- |
| [`/`](https://gdg-vibecoding.vercel.app) | 강의 안내와 커리큘럼, 각 페이지 바로가기 |
| [`/slide`](https://gdg-vibecoding.vercel.app/slide/) | 발제 슬라이드 53장. 강의실 프로젝터용 |
| [`/prompt`](https://gdg-vibecoding.vercel.app/prompt/) | 수업 중 붙여넣을 프롬프트 40개 + 실행 예시 사진 30장 |
| [`/share`](https://gdg-vibecoding.vercel.app/share/) | 배포한 내 주소를 올리고 서로 구경하는 곳 |
| [`/idea`](https://gdg-vibecoding.vercel.app/idea/) | 질문에 답하면 AI에 붙여넣을 프롬프트가 나오는 브레인스토밍 |
| [`/assignment`](https://gdg-vibecoding.vercel.app/assignment/) | 불참자용 대체 과제 안내 |

<!-- 스크린샷: 각 페이지 -->

### `/slide` — 발제 슬라이드

<!-- 스크린샷: 슬라이드 -->

- 슬라이드 하나는 `<section class="slide">` 하나. **JS가 개수를 자동으로 세기 때문에** 추가·삭제·순서 변경만 하면 진행바와 목차가 따라옵니다
- <kbd>←</kbd> <kbd>→</kbd> 이동, <kbd>O</kbd> 목차, 좌우 클릭·스와이프 지원
- `#12` 처럼 주소에 번호를 붙이면 그 장으로 바로 들어갑니다. 발표 중 특정 슬라이드로 점프할 때 씁니다
- 1280×720 · 1920×1080 **전 장이 스크롤 없이** 한 화면에 들어가도록 맞춰져 있습니다

### `/prompt` — 프롬프트 모음

<!-- 스크린샷: 프롬프트 -->

- 파트별 13개 아코디언, 기본은 전부 접힘
- **터미널**(셸에 치는 것) / **프롬프트**(AI에게 하는 말) / **명령**(`/` 로 시작) 세 가지를 색으로 구분
- 블록마다 복사 버튼. 클립보드가 막히면 `execCommand` 로 한 번 더 시도하고, 그래도 안 되면 **블록을 선택 상태로 만들어** 직접 복사할 수 있게 합니다
- 단계마다 실제 실행 화면 사진을 접이식으로. 눌러서 원본 크기로 볼 수 있습니다

### `/share` — 결과물 공유

<!-- 스크린샷: 공유 -->

- 이름 · 링크 · 한 줄 설명을 올리면 카드로 쌓이고, 누르면 새 탭으로 그 사이트가 열립니다
- `example.com` 처럼 적어도 **`https://` 를 자동으로 붙여** 저장합니다
- 같은 주소는 한 번만. 중복이면 "이미 등록된 링크입니다"
- 수정·삭제는 막아두었습니다. Supabase RLS로 `select` / `insert` 만 열려 있습니다

### `/idea` — 브레인스토밍

<!-- 스크린샷: 아이디어 -->

뭘 만들지 못 정한 사람이 질문에 하나씩 답하면, 그대로 AI에 붙여넣을 프롬프트가 조립됩니다.

```
[맥락 선택] → [출발점 선택] → [질문] → [프롬프트 조립]
```

- **맥락** 세 가지 — 해커톤 / 사이드 프로젝트 / 아이템 탐색. 고른 값이 뒤따르는 질문의 기한·성공 장면·접근 가능성 문구를 바꿉니다
- **출발점** 두 가지 — 직접 겪는 불편(7문항) / 옆에서 본 것(5문항)
- 답이 20자 미만이면 다음으로 못 넘어갑니다. 구체적인 경험이 안 나오면 프롬프트가 쓸모없어지기 때문입니다
- **접근 가능성 게이트** — "그분께 물어볼 수 없다"를 고르면 진행을 막고 트랙 변경을 권합니다. 만날 수 없는 사람의 문제는 결국 상상으로 만들게 되니까요
- 답변은 `localStorage` 에 저장되어 새로고침해도 이어집니다

### `/assignment` — 대체 과제

특강에 참여하지 못한 사람이 강의 내용을 따라 직접 배포하고 `/share` 에 올리는 과정을 안내합니다. Vercel의 **긴 배포 주소는 기본적으로 로그인 화면이 뜨기 때문에**, 짧은 주소를 시크릿 창에서 확인하는 절차를 따로 강조해두었습니다.

---

## 커리큘럼

| | 내용 | 시간 |
| --- | --- | --- |
| **Part 1** | 바이브코딩 · Agentic Loop · 라이브 데모 | 15분 |
| **Part 2** | 폴더 만들고 `claude` 실행 | 5분 |
| **Part 3** | CLAUDE.md → Plan 기획 → 구현 → 배포 → 디자인 재배포 | 45분 |
| 쉬는시간 | 자기 URL 공유하고 서로 구경 | 10분 |
| **Part 4** | MCP 연결 · Skills · 플러그인 | 20분 |
| **Part 5** | 컨텍스트 관리 · 디버깅 | 10분 |
| **Part 6** | 그래서 좋은 걸 만들려면 | 10분 |
| 자율 실습 | 각자 자기 문제로 | 25분 |
| Q&A | | 10분 |

배포(Part 3)를 의도적으로 앞으로 당겼습니다. **자기 주소가 나오는 순간이 이 강의의 하이라이트**인데 맨 뒤에 두면 시간이 밀렸을 때 통째로 날아가기 때문입니다. 디자인 작업을 배포 직후에 붙인 것도 같은 이유 — 주소를 열어본 순간의 "밋밋한데?"를 바로 받아치기 위해서입니다.

---

## 기술 구성

빌드 도구가 없습니다. HTML을 열면 그게 그대로 배포본입니다.

| | |
| --- | --- |
| 호스팅 | Vercel 정적 배포 |
| 폰트 | Pretendard (CDN) |
| DB | Supabase — `/share` 의 등록·목록에만 사용 |
| 빌드 | 없음. 프레임워크·번들러·패키지 매니저 모두 없음 |

**왜 빌드 도구를 안 썼나**
수강생 절반이 휴대폰으로 보고, 강의 도중에도 문구를 고쳐야 합니다. 빌드 단계가 있으면 그때마다 파이프라인이 걸립니다. 페이지당 HTML 한 장을 유지하면 고치고 바로 올릴 수 있습니다.

**디자인 규칙**
흰 배경(`#FFFFFF`)과 크림슨(`#801A2C`) 두 축, 폰트는 Pretendard 하나. 색과 폰트를 묶어두면 페이지를 새로 만들어도 같은 얼굴이 나옵니다.

<details>
<summary><b>폰트를 하나로 줄인 이유</b></summary>

원래는 Wanted Sans(본문) + JetBrains Mono(코드) 두 벌이었습니다. 두 가지 문제가 있었습니다.

- **Wanted Sans** — CDN 경로가 404라 처음부터 로드된 적이 없었습니다. 본문이 계속 브라우저 기본 폰트로 렌더링되고 있었습니다
- **JetBrains Mono** — 한글 글리프가 없습니다. 커리큘럼 표의 「쉬는시간」·「자율 실습」이 시스템 한글 폰트로 떨어지면서 그 줄만 튀어 보였습니다

지금은 Pretendard 하나로 통일하고, 코드·라벨은 자간 0과 `tabular-nums` 로만 본문과 구분합니다.

</details>

---

## 파일 구조

```
index.html              메인
slide/index.html        발제 슬라이드
prompt/index.html       프롬프트 모음
share/index.html        결과물 공유
idea/index.html         브레인스토밍
assignment/index.html   대체 과제 안내
styles.css              공통 디자인 토큰
img/                    실행 예시 사진 30장
```

`styles.css` 의 `:root` 토큰과 리셋은 모든 페이지가 함께 씁니다. `.page` 로 시작하는 규칙은 읽는 페이지 전용이고, `/slide` 는 `body` 에 `.page` 가 없어 영향을 받지 않습니다. 슬라이드 전용 스타일은 `slide/index.html` 안에 그대로 둡니다.

---

## 로컬에서 보기

빌드가 없으니 정적 서버 하나면 됩니다.

```bash
python -m http.server 4321
```

`/share` 는 Supabase anon key를 `share/index.html` 안에 직접 두고 있습니다. 빌드 도구가 없어 환경변수를 주입할 방법이 없기 때문이고, **anon key는 원래 클라이언트에 노출되는 공개 키**입니다. 테이블은 RLS로 `select` / `insert` 만 열려 있어 `update` / `delete` 는 막혀 있습니다.

---

<div align="center">

**GDGoC Korea Univ.** · 바이브코딩 특강

</div>

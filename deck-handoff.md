# 바이브코딩 특강 사이트 — 작업 인수인계

Claude Code에 이 문서를 통째로 넘겨도 된다.
**실제로 할 일은 C섹션 하나뿐이다.** 나머지는 규칙과 기록이다.

시작하기 전에 **G섹션(시작 전 준비)** 을 먼저 볼 것. Supabase 키가 없으면 C-4에서 멈춘다.

---

## A. CLAUDE.md 에 넣을 내용

아래 블록을 그대로 `CLAUDE.md`로 저장한다.

```markdown
# 바이브코딩 특강 사이트

## 무엇
2026 GDGoC Korea Univ. 바이브코딩 특강(150분)의 공식 사이트.
Vercel 정적 배포. 빌드 도구 없음. 외부 의존은 폰트 CDN과 Supabase JS뿐.

## 페이지
/           메인 — 강의 안내 + 하위 페이지 바로가기
/slide      발제 슬라이드 (강의실 프로젝터용)
/prompt     수업 중 CLI에 입력할 프롬프트 모음
/share      수강생이 자기 결과물 링크를 올리고 서로 보는 곳

## 대상
코딩을 처음 하는 대학교 1~4학년.
/slide 는 강의실 프로젝터로 본다. 뒷자리에서 읽혀야 하므로
글자 크기를 줄이는 방향의 수정은 적당히만 진행할 것.
나머지 페이지는 절반이 휴대폰으로 본다. 모바일에서 반드시 읽혀야 한다.

## 디자인 규칙 (전 페이지 공통)
- 색은 흰 배경(#FFFFFF)과 크림슨(#801A2C) 두 가지만. 다른 색 추가 금지
- 폰트: Wanted Sans(본문/제목), JetBrains Mono(코드). 변경 금지
- 표(`<table>`) 쓰지 말 것. `.rows`(용어+설명) 또는 `.vs`(2열 비교)를 쓴다
- 애니메이션 추가 금지

## 슬라이드(/slide) 전용 규칙
- 슬라이드 단위는 `<section class="slide"><div class="wrap">…</div></section>`
- JS가 개수를 자동으로 세므로 추가·삭제·순서변경만 하면 된다
- 진행 레일, 목차(O키), 진행바는 자동 생성. 건드릴 필요 없음
- 한 슬라이드에 한 아이디어. 강조는 슬라이드당 한 군데만
- `h2.solo` 슬라이드는 문장 하나만 두는 자리다. 말로 채우는 구간이니 내용을 덧붙이지 말 것

## 하지 말 것
- localStorage / sessionStorage 사용 금지
- 슬라이드 내용을 임의로 고치지 말 것. 순서와 문구는 확정된 상태다
- 프레임워크 도입 금지. 페이지당 HTML 한 장을 유지한다
- Supabase 키를 코드에 하드코딩하지 말 것

## 관련 문서
- 노션 「바이브코딩 특강 가이드 (수강생용)」 — 내용 원본
- 노션 「바이브코딩-특강-강의계획안」 — 시간 배분과 강사 메모
```

---

## B. 확정된 커리큘럼 순서

| | 내용 | 시간 |
| --- | --- | --- |
| Part 1 | 바이브코딩 · Agentic Loop · 라이브 데모 | 15분 |
| Part 2 | 폴더 만들고 claude 실행 | 5분 |
| Part 3 | CLAUDE.md → Plan 기획 → 구현 → 배포 → 디자인 재배포 | 45분 |
| 쉬는시간 | 자기 URL 공유하고 서로 구경 | 10분 |
| Part 4 | MCP 연결 · Skills · Hooks · Sub-agents | 20분 |
| Part 5 | 컨텍스트 관리 · 디버깅 | 10분 |
| Part 6 | 그래서 좋은 걸 만들려면 | 10분 |
| 자율 실습 | 각자 자기 문제로 | 25분 |
| Q&A | | 10분 |

---

## C. 할 일 — 사이트 4페이지로 확장

지금 폴더의 `index.html`은 발제 슬라이드이고, Vercel에 배포되어 있다.
이걸 4개 페이지짜리 사이트로 확장한다.

### 진행 방식

먼저 `/plan` 으로 파일 구조와 작업 순서를 보여준다.
그 뒤로는 **중간에 멈추지 말고 C-1부터 C-4까지 이어서 진행한다.**
단계마다 커밋을 따로 남기고, 전부 끝나면 무엇을 했고 무엇이 확인 필요한지 요약한다.

막히면 멈추지 말고, 그 부분만 건너뛰고 나머지를 끝낸 뒤 마지막 요약에 적는다.

### 최종 파일 구조

```
index.html            메인
slide/index.html      기존 슬라이드
prompt/index.html     프롬프트 모음
share/index.html      결과물 공유
styles.css            4개 페이지가 공유하는 디자인 토큰
```

Vercel 정적 배포이므로 폴더 방식으로 라우팅한다.

---

### C-1. 슬라이드 이동

지금 `index.html`을 `slide/index.html`로 옮긴다.
**내용은 한 글자도 건드리지 말 것.**

- 슬라이드 안의 해시 링크(`#3` 같은 것)가 그대로 동작하는지 확인
- 좌하단 "GDGoC Korea Univ." 자리에 메인(`/`)으로 돌아가는 링크를 단다
- 공통 스타일을 `styles.css`로 빼되, 슬라이드 전용 스타일은 그대로 둔다

---

### C-2. 메인 페이지

아래 내용으로 `index.html`을 새로 만든다.

**[히어로]**
```
바이브 코딩
Claude Code로 웹 서비스 만들고 배포하기
2026. 9. 10. 18:00 · GDGoC Korea Univ.
```

**[바로가기 버튼 3개]** — 히어로 바로 밑. 페이지에서 가장 눈에 띄어야 한다.
수업 중 이 사이트에 오는 이유는 거의 다 프롬프트를 복사하기 위해서다.
```
슬라이드 보기    /slide
프롬프트 모음    /prompt
결과물 공유      /share
```

**[이 강의는]**
```
AI Agent를 이용하는 개발 패러다임인 바이브 코딩을 다룹니다.
단순히 AI에게 코드를 짜달라고 요청하는 방식을 넘어, CLI와 Agentic Loop의
동작 방식을 이해하고, 컨텍스트 엔지니어링과 MCP를 거쳐
아이디어를 실제 배포까지 끌고 가는 한 사이클을 경험합니다.
```

**[이런 분께]**
```
- LLM과 사이트에서만 대화하며 코드를 만들어보신 분
- CLI 환경을 빠르게 세팅하고 바로 써먹고 싶은 분
- MCP와 에이전트 확장 기능을 직접 실습해보고 싶은 분
- 기획부터 배포까지 하루 만에 완결 짓는 경험을 원하는 분
- 바이브 코딩 초보자
```

**[오늘 하는 것]** — B섹션 표를 그대로. 표 대신 `.rows` 형식으로.

**[준비물]**
```
- 개인 노트북
- Claude Pro 요금제 이상
- GitHub 계정 / git 설치
```

**[강사]**
```
Seungmin Yeo · 고려대학교 인공지능학과 25학번
정보대학 제3회 InThon 데이터톤 금상
LG Aimers 8기 해커톤 2등
고려대학교 딥러닝학회 AIKU 7기
GDGKU 학술팀
```

---

### C-3. /prompt

수업 중 터미널에 붙여넣을 프롬프트 모음.
수강생이 강의를 놓쳤을 때 여기 와서 복사해간다.

**요구사항**
- 파트별 섹션으로 나누고 `<details>`/`<summary>`로 접었다 펼 수 있게
- 기본은 전부 접힌 상태
- 프롬프트 블록마다 "복사" 버튼. 누르면 클립보드에 들어가고 버튼 글씨가 "복사됨"으로 잠깐 바뀜
- 터미널 명령(bash)과 Claude에게 하는 말(프롬프트)을 시각적으로 구분
- 모바일에서 복사 버튼이 눌리는 크기여야 함

**들어갈 내용 — 순서 그대로**

```
### 00. 시작 전 확인
[bash]
claude --version
git --version
gh --version

### Part 2. 폴더 만들고 시작하기
[bash] macOS / Linux
mkdir -p ~/dev/my-project
cd ~/dev/my-project
claude

[bash] Windows
mkdir dev\my-project
cd dev\my-project
claude

### Part 3-1. CLAUDE.md 세팅
[프롬프트]
내 소개 페이지를 하나 만들려고 해. CLAUDE.md만 우선 세팅해줘.

- Next.js + TypeScript + Tailwind
- 한 페이지짜리. 소개 글, 관심사, 링크 정도면 충분해
- 데이터는 로컬 JSON 파일로 (DB는 나중에 붙일 거야)
- Vercel 배포나 Supabase 연동은 지금 하지 마
- 위 내용을 CLAUDE.md에 정리해줘

### Part 3-2. Plan 모드로 기획
[명령] /plan

[프롬프트]
[여기에 노션 내용 붙여넣기]

이 내용으로 내 소개 페이지를 설계해줘.
파일은 아직 만들지 말고 계획만 보여줘.

### Part 3-3. 구현
[프롬프트]
이대로 진행해줘

### Part 3-4. 로컬 확인
[프롬프트]
localhost:3000으로 서버 실행해서 잘 뜨는지 확인해줘

[프롬프트] 에러가 났을 때
이 에러 고쳐줘

### Part 3-5. GitHub와 배포
[bash]
gh auth login

[프롬프트]
이 프로젝트 git 초기화하고, GitHub에 0910VibeCoding-[내이름] 이라는 이름으로
퍼블릭 레포 만들어서 커밋하고 푸시해줘

### Part 3-6. 디자인 입히고 재배포
[프롬프트]
[claude.ai/design 에서 만든 화면 캡처를 Ctrl+V]

이 시안대로 내 페이지 스타일 바꿔줘.
내용은 그대로 두고 색이랑 레이아웃만.

[프롬프트]
커밋하고 푸시해줘

### Part 4-1. MCP 연결
[bash] Claude Code 밖 터미널에서
claude mcp add --transport http notion https://mcp.notion.com/mcp

[명령] claude 다시 실행한 뒤
/mcp

[프롬프트]
내 Notion의 '[페이지 제목]' 페이지 읽어서 요약해줘

[프롬프트]
내 Notion 페이지 다시 읽어서 사이트 내용 업데이트하고, 커밋·푸시해줘

### Part 4-2. Skills 만들기
[프롬프트]
.claude/skills/ 아래에 project-review 스킬을 하나 만들어줘.
내 프로젝트를 읽고 아래 세 가지를 순서대로 답하는 스킬이야.

1. 이 서비스가 없으면 사용자는 지금 뭐로 해결하고 있나? 그것보다 나은가?
2. 화면에 있는 기능 중, 없어도 되는 건 뭔가?
3. 이 기술을 왜 썼나? 더 단순한 방법은 없었나?

각 판단에는 확인한 파일이나 구체적인 근거를 같이 적어줘.
용어는 CLI를 처음 쓰는 사람 수준으로.

[명령] Claude Code 다시 켜고
/project-review

### Part 5. 막혔을 때 쓰는 명령어
[명령] /context   기억을 몇 % 썼는지
[명령] /compact   대화 요약해서 줄이기
[명령] /clear     대화 통째로 비우기
[명령] /rewind    이전 시점으로 되돌리기
[명령] /memory    CLAUDE.md 고치기

[프롬프트] 원인을 모를 때
방금 뭐 바꿨는지 보여줘

[프롬프트] 여러 개 고친 뒤 깨졌을 때
하나씩 되돌려보면서 어디서 깨지는지 찾아줘

### 자율 실습
[프롬프트] 기술 선택을 점검할 때
이거 왜 이 기술로 했어? 더 단순한 방법은 없어?
```

---

### C-4. /share

수강생이 자기 배포 주소를 올리고, 서로 구경하는 페이지.

**화면**
- 입력 폼: 이름 / 링크 / 한 줄 설명
- 아래에 등록된 것들이 카드로 쭉. 최신순
- 카드를 누르면 새 탭으로 그 사이트가 열린다

**저장 — Supabase**

`@supabase/supabase-js` 를 CDN으로 불러 쓴다. 빌드 없이 동작해야 한다.

테이블 하나면 된다.
```sql
create table shares (
  id          bigint generated always as identity primary key,
  name        text not null,
  url         text not null unique,
  description text,
  created_at  timestamptz default now()
);

alter table shares enable row level security;

create policy "anyone can read"   on shares for select using (true);
create policy "anyone can insert" on shares for insert with check (true);
```

키는 `.env.local`에 두고, Vercel 환경변수에도 같은 이름으로 등록한다.
정적 페이지이므로 anon key는 클라이언트에 노출된다. 이건 정상이다.
RLS로 select와 insert만 열어두었으니 update/delete는 막혀 있다.

**주의할 것**
- 링크가 `http://` 또는 `https://` 로 시작하는지 검사하고, 아니면 앞에 `https://` 를 붙인다
- 이름 20자, 설명 80자로 제한
- 같은 URL이 이미 있으면 중복 등록을 막고, 이미 등록된 링크라고 알려준다
- 등록 직후 목록이 바로 갱신되어야 한다
- 삭제 기능은 만들지 않는다. 대신 "수정이나 삭제가 필요하면 강사에게 말씀해주세요" 한 줄 안내
- 아직 아무도 등록하지 않았을 때의 빈 화면 문구를 넣는다. "첫 번째로 올려보세요" 정도

---

## D. 지난 수정 기록 — 이미 반영됨. 작업하지 말 것

> 아래는 슬라이드가 지금 순서로 정해진 경위다.
> **이미 전부 반영되어 있다. 다시 작업하지 말 것.**
> 왜 이 순서가 됐는지 알아야 할 때만 읽으면 된다.

**커리큘럼이 바뀐 지점**

1. 배포가 맨 뒤에서 Part 3 안으로 들어왔다 — 자기 주소가 나오는 순간이 하이라이트인데 마지막에 있으면 밀렸을 때 통째로 날아가서
2. Claude Design 스타일 작업이 배포 직후에 생겼다 — 주소를 열어본 순간의 실망을 바로 받아치려고
3. MCP·Skills가 배포 뒤로 밀렸다 — "이미 떠 있는 사이트를 고치는 도구"로 읽히게
4. 컨텍스트·디버깅이 자율 실습 직전으로 왔다 — 한참 해본 뒤에 설명해야 먹히고, 배우자마자 쓰게 되니까
5. 자율 실습이 35분에서 25분으로 줄었다

**반영 완료된 슬라이드 수정**

- 배포 3장(`GitHub 용어 세 개` / `레포 만들고 올리기` / `배포하기`)을 `알아두면 편한 명령어` 뒤로 이동
- Claude Design 3장 신규 추가
- 쉬는시간 슬라이드 1장 추가
- `노션 연결하기` 슬라이드에 재배포 프롬프트 추가
- 컨텍스트·디버깅 7장을 플러그인 뒤로 이동
- 자율 실습 1장 → 3장 (종이 세 줄 / 지우기 / 30초 발표)
- 각 파트 첫 장에 `Part 1`~`Part 6` 표시 추가

---

## E. 검수 항목

작업이 끝나면 확인한다.

**전체**
- 4개 페이지가 모두 열리고 서로 이동이 되는지
- 크림슨과 흰색 외의 색이 들어가지 않았는지
- 모바일 폭(390px)에서 네 페이지 모두 읽히는지

**/slide**
- 슬라이드 총 개수가 우하단 카운터와 맞는지 (현재 48장)
- `O` 키 목차 순서가 B섹션 커리큘럼과 일치하는지
- 1280×720 및 1920×1080에서 모든 슬라이드가 스크롤 없이 들어가는지

**/prompt**
- 복사 버튼이 실제로 클립보드에 넣는지
- 모든 섹션이 접힌 상태로 시작하는지

**/share**
- 등록이 되고 목록에 바로 뜨는지
- 잘못된 링크(`example.com`)를 넣었을 때 `https://` 가 붙는지
- 같은 URL을 두 번 넣었을 때 막히는지

---

## F. 아직 정해지지 않은 것

- 강의실 와이파이가 불안할 경우 폰트를 로컬로 내려받아 번들할지
- 발표자 노트 기능 추가 여부 (현재 미구현)
- 쉬는시간 슬라이드에 시간과 안내 문구를 넣을지 (현재 「쉬는시간」 한 단어)

---

## G. 시작 전 준비 — 이것만 사람이 해야 한다

C-4에서 Supabase 키가 필요하다. **없으면 거기서 멈춘다.**
자리를 비우기 전에 아래를 먼저 해두고 `.env.local`을 만들어 둘 것.

1. supabase.com → New Project, 리전은 **Seoul (ap-northeast-2)**
2. 생성에 2~3분 걸린다
3. Settings → API 에서 **Project URL** 과 **anon public key** 복사
4. 프로젝트 폴더에 `.env.local` 생성

```
NEXT_PUBLIC_SUPABASE_URL=여기에_Project_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=여기에_anon_key
```

5. `.gitignore` 에 `.env.local` 이 있는지 확인
6. Vercel 프로젝트 설정 → Environment Variables 에도 같은 두 개를 등록

테이블 생성 SQL은 C-4에 있다. Supabase 대시보드의 SQL Editor에 붙여넣으면 된다.
Claude Code에게 시켜도 되지만, 직접 하는 편이 확실하다.

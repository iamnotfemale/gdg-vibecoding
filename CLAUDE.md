# 바이브코딩 특강 사이트

## 무엇
2026 GDGoC Korea Univ. 바이브코딩 특강(150분)의 공식 사이트.
Vercel 정적 배포. 빌드 도구 없음. 외부 의존은 폰트 CDN과 Supabase JS뿐.

## 페이지
/           메인 — 강의 안내 + 하위 페이지 바로가기
/slide      발제 슬라이드 (강의실 프로젝터용)
/prompt     수업 중 CLI에 입력할 프롬프트 모음
/share      수강생이 자기 결과물 링크를 올리고 서로 보는 곳

## 대상
코딩을 처음 하는 대학교 1~4학년.
/slide 는 강의실 프로젝터로 본다. 뒷자리에서 읽혀야 하므로
글자 크기를 줄이는 방향의 수정은 적당히만 진행할 것.
나머지 페이지는 절반이 휴대폰으로 본다. 모바일에서 반드시 읽혀야 한다.

## 디자인 규칙 (전 페이지 공통)
- 색은 흰 배경(#FFFFFF)과 크림슨(#801A2C) 두 가지만. 다른 색 추가 금지
- 폰트는 Pretendard 하나뿐. 다른 폰트 추가 금지
  (Wanted Sans 는 CDN 경로가 404 라 로드된 적이 없었고,
   JetBrains Mono 는 한글 글리프가 없어 한글이 시스템 폰트로 떨어졌다.
   코드·라벨은 같은 폰트에 자간 0 과 tabular-nums 로만 구분한다)
- 표(`<table>`) 쓰지 말 것. `.rows`(용어+설명) 또는 `.vs`(2열 비교)를 쓴다
- 슬라이드(/slide)에는 애니메이션 추가 금지. 이미 있는 등장 효과로 충분하다
- 메인과 /prompt 에는 스크롤 등장(.reveal)과 아코디언 펼침만 허용한다.
  둘 다 prefers-reduced-motion 을 존중해야 하고,
  스크립트가 실패해도 내용이 보이는 구조여야 한다

## 슬라이드(/slide) 전용 규칙
- 슬라이드 단위는 `<section class="slide"><div class="wrap">…</div></section>`
- JS가 개수를 자동으로 세므로 추가·삭제·순서변경만 하면 된다
- 진행 레일, 목차(O키), 진행바는 자동 생성. 건드릴 필요 없음
- 한 슬라이드에 한 아이디어. 강조는 슬라이드당 한 군데만
- `h2.solo` 슬라이드는 문장 하나만 두는 자리다. 말로 채우는 구간이니 내용을 덧붙이지 말 것

## 하지 말 것
- localStorage / sessionStorage 사용 금지
- 슬라이드 내용을 임의로 고치지 말 것. 순서와 문구는 확정된 상태다
- 프레임워크 도입 금지. 페이지당 HTML 한 장을 유지한다
- Supabase service_role key 는 절대 코드에 넣지 말 것
  (anon key 는 빌드 도구가 없어 주입할 방법이 없으므로 share/index.html 안에 직접 둔다.
   원래 공개되는 키이고 RLS 로 막혀 있다)

## 파일 구조
```
index.html            메인
slide/index.html      발제 슬라이드
prompt/index.html     프롬프트 모음
share/index.html      결과물 공유
img/                  노션 「실행 예시 사진」 토글에서 받아온 스크린샷
styles.css            4개 페이지가 공유하는 디자인 토큰
```

`styles.css` 의 `:root` 토큰과 리셋은 4개 페이지 전부가 쓴다.
`.page` 로 시작하는 규칙은 읽는 페이지(메인/프롬프트/공유) 전용이며,
/slide 는 body 에 `.page` 가 없으므로 영향을 받지 않는다.
슬라이드 전용 스타일은 `slide/index.html` 안에 그대로 둔다.

## 관련 문서
- 노션 「바이브코딩 특강 가이드 (수강생용)」 — 내용 원본
- 노션 「바이브코딩-특강-강의계획안」 — 시간 배분과 강사 메모
- `deck-handoff.md` — 이 사이트를 4페이지로 확장한 작업 지시서

## 노션 이미지
`img/` 는 노션 본문의 이미지 링크를 그대로 걸 수 없어서 받아둔 것이다.
그 링크는 5분짜리 presigned URL(`X-Amz-Expires=300`)이라 금방 깨진다.
노션에서 사진이 바뀌면 다시 받아 `img/` 를 갈아끼워야 한다.
